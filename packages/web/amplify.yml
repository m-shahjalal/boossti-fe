version: 1
applications:
  - frontend:
      framework: nextjs
      path: packages/web
      build:
        commands:
          - npm install
          - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
